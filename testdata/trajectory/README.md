Fictional runner records for `tests/reporting/test_trajectory.py` and `tests/reporting/test_measure.py`, generated once.
`root-a` and `root-b` overlap (copied run directories); expected totals were computed by hand.
