Fictional runner records for the run summary comments (`tests/linear/test_run_summary.py`) and
`render_samples.py`, generated once. TEAM-12 has implement, a failed validation, one repair in the same
session and review; TEAM-13 has a failed implement turn without a counter, then a blocked one (its
delta is an upper bound); TEAM-11 was implemented by Claude (per-invocation counters). Check logs are
not included.
