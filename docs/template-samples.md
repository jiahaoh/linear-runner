# Linear comment samples

Generated by `python3 render_samples.py` from fictional fixture data; do not edit by hand. Each sample below is exactly the comment body Linear would receive for that template, including the one hidden marker line at the end (an HTML comment Linear does not display). Paths, issue IDs, the owner handle and model names are placeholders. Template wording lives in `templates/`.

| # | Sample | Template | Author | Posted |
| --- | --- | --- | --- | --- |
| 1 | Claim | `templates/claim.md` | runner | when work on an issue starts |
| 2 | Progress | `templates/draft-progress.md` | worker | as soon as the runner sees the draft, while Codex still runs |
| 3 | Ready for validation | `templates/draft-ready.md` | worker | when the session ends with status ready |
| 4 | Ready for validation, runner fallback | `templates/ready.md` | runner | instead of the worker's note when it is missing or fails the lint |
| 5 | Validation result | `templates/validation.md` | runner | after each validation run (passed, or failed with a repair next) |
| 6 | Review result | `templates/draft-review.md` | reviewer | when the independent review accepts the work |
| 7 | Review result, runner fallback | `templates/review.md` | runner | instead of the reviewer's note when it fails the lint |
| 8 | Done, with deliverables to review | `templates/done.md` | runner | after Done is published and read back |
| 9 | Blocked or stopped, with the worker's blocked note | `templates/blocked.md` + `templates/draft-blocked.md` | runner (quotes the worker) | when the batch pauses; the issue also gets the Needs input label |
| 10 | Blocked or stopped, review not accepted | `templates/blocked.md` | runner (quotes the reviewer) | when the batch pauses on a rejected review |
| 11 | Blocked or stopped, environment | `templates/blocked.md` | runner | when a host or service problem pauses the batch |
| 12 | Issue deferred | `templates/deferred.md` | runner | when a decision rule or on_block policy sets an issue aside |
| 13 | Recovery recorded | `templates/recovery.md` | runner | when a launch carries out a recorded recovery |
| 14 | Batch finished | `templates/batch-finished.md` | runner | on the terminal issue and report issues when the batch ends |
| 15 | Batch paused | `templates/batch-paused.md` | runner | on the terminal issue and report issues when the batch pauses |
| 16 | Watchdog alert | `templates/watchdog.md` | runner (model-free watchdog) | when the supervisor vanished without an outcome, or stalled |
| 17 | Done, nothing to review | `templates/done.md` | runner | after Done is published and read back |

## 1. Claim

Template `claim.md`, written by the runner, posted when work on an issue starts.

---

Work on TEAM-12 has started; no action is needed.

**Plan**
The worker implements TEAM-12 with the Standard profile (model-a, medium effort). The runner then runs 2 configured checks, commits, and asks an independent reviewer to assess all 3 acceptance criteria. Worker progress notes will appear here as separate comments.

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d

<!-- linear-runner demo-batch/TEAM-12/claim/1 -->

---

## 2. Progress

Template `draft-progress.md`, written by the worker, posted as soon as the runner sees the draft, while Codex still runs.

---

The spot-count section of the QC report now renders for all tiles; no action is needed.

**What changed**
I added per-tile spot counts to the QC report and a small helper that computes bootstrap error bars. The report renders for the fixture dataset in about ten seconds.

**Next**
Wire the calibration table into the pipeline and run the focused tests.

**Risks**
The calibration table may not be in the configured data folder; I will check next.

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/qc_report.html

_Written by the worker (implement phase); posted by the runner._

<!-- linear-runner demo-batch/TEAM-12/progress/1 -->

---

## 3. Ready for validation

Template `draft-ready.md`, written by the worker, posted when the session ends with status ready.

---

TEAM-12 is ready for validation: the QC report has per-tile error bars and the pipeline reads the calibration table; no action is needed.

**What was done**
Per-tile spot counts with bootstrap error bars are in the QC report. The pipeline now reads calibration.csv from the configured data folder instead of hard-coded values.

**How it was checked**
I ran the focused QC tests and the full regression suite once; both passed.

**Limitations**
Error bars use 200 bootstrap samples to keep the report fast.

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/implement-20260923T101500Z-5e6f7a8b

_Written by the worker (implement phase); posted by the runner._

<!-- linear-runner demo-batch/TEAM-12/ready/1 -->

---

## 4. Ready for validation, runner fallback

Template `ready.md`, written by the runner, posted instead of the worker's note when it is missing or fails the lint.

---

The worker reports TEAM-12 ready for validation; no action is needed.

**Worker summary**
> Added per-tile spot counts with bootstrap error bars to the QC report and read the calibration table from the configured data folder.

**Criteria**
The worker marked 3 of 3 acceptance criteria as met; the runner now runs the checks. The worker's own ready note was not posted because the first paragraph must be exactly one sentence ending with '.', '!' or '?'.

**Deliverables to review**
- /absolute/path/to/worktrees/demo-batch/reports/qc_report.html — QC report with per-tile spot counts and error bars
- /absolute/path/to/worktrees/demo-batch/docs/calibration.md — How the calibration table is read

**Limitations**
Error bars use 200 bootstrap samples; more would take several minutes per tile.

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/implement-20260923T101500Z-5e6f7a8b

<!-- linear-runner demo-batch/TEAM-12/ready/2 -->

---

## 5. Validation result

Template `validation.md`, written by the runner, posted after each validation run (passed, or failed with a repair next).

---

1 of 2 checks failed for TEAM-12; the runner is starting repair 1 of 2, so no action is needed yet.

**Failing checks**
regression exited with code 1.

**Repair**
The repair worker gets the failing check logs and may change only what those checks need.

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/validation-20260923T103000Z-0a1b2c3d

<!-- linear-runner demo-batch/TEAM-12/validation/1 -->

---

## 6. Review result

Template `draft-review.md`, written by the reviewer, posted when the independent review accepts the work.

---

I accept TEAM-12: all three criteria are met and nothing needs the owner's attention.

**Assessment**
The QC report shows per-tile spot counts with error bars, and the values match a manual bootstrap on two tiles. The pipeline reads the calibration table from the configured folder, and the regression suite passes on the committed revision.

**Concerns**
The error bars use 200 bootstrap samples; that is fine for QC but not for publication figures.

_Written by the reviewer (review phase); posted by the runner._

<!-- linear-runner demo-batch/TEAM-12/review/1 -->

---

## 7. Review result, runner fallback

Template `review.md`, written by the runner, posted instead of the reviewer's note when it fails the lint.

---

The independent review accepted all 3 acceptance criteria for TEAM-12; the runner is publishing it as Done, so no action is needed.

**Reviewer summary**
> The committed work meets all three criteria and the report reads well. The error bars match a manual bootstrap on two tiles.

The reviewer's note did not follow its template (missing required section(s): Assessment), so it is quoted here.

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/review-20260923T110000Z-4d5e6f7a

<!-- linear-runner demo-batch/TEAM-12/review/2 -->

---

## 8. Done, with deliverables to review

Template `done.md`, written by the runner, posted after Done is published and read back.

---

TEAM-12 is Done: the independent review accepted all 3 acceptance criteria and Linear shows the issue Done; the deliverables below are ready for your review.

**Deliverables to review**
- /absolute/path/to/worktrees/demo-batch/reports/qc_report.html — QC report with per-tile spot counts and error bars
- /absolute/path/to/worktrees/demo-batch/docs/calibration.md — How the calibration table is read
- /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/delivery/packet/review.html — file from the delivery packet

**Delivered**
Commit 4c44464d4ce9 on branch codex/demo-batch, local only (not pushed or merged). It needed 1 repair before the checks passed.

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d, /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/final-result.json

<!-- linear-runner demo-batch/TEAM-12/done/1 -->

---

## 9. Blocked or stopped, with the worker's blocked note

Template `blocked.md + draft-blocked.md`, written by the runner (quotes the worker), posted when the batch pauses; the issue also gets the Needs input label.

---

TEAM-12 is paused because the worker reported that it cannot finish, and it needs your decision to continue.

**What happened**
The worker ended its implement session with status blocked instead of ready.

**In their own words**
The worker wrote:

> I cannot finish TEAM-12 because the calibration table it names is not in the data folder, and I need the owner to say where it lives.
>
> **What is blocking**
> The second criterion asks the pipeline to read the calibration table from the configured data folder. That folder only has raw tiles, and the older pipeline hard-coded the values.
>
> **What was tried**
> I searched the data folder and the project notes and found no calibration file.
>
> **What is needed**
> Either the path of the calibration table or permission to write one from the hard-coded values.

**What is needed**
Decide whether to clarify the criterion, give the worker a note, or set the issue aside.

**To continue**
Record the recovery (you can add --note-file with a note for the worker):

```bash
python3 /absolute/path/to/linear-runner/runner.py recover resume --batch demo-batch --reason "<why>" --authorized-by "<your name>"
```

Then start the batch again:

```bash
python3 /absolute/path/to/linear-runner/runner.py launch --batch demo-batch
```

Or set the issue aside instead and let the batch continue:

```bash
python3 /absolute/path/to/linear-runner/runner.py recover defer --issue TEAM-12 --restore-worktree --batch demo-batch --reason "<why>" --authorized-by "<your name>"
```

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d, /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/implement-20260923T101500Z-5e6f7a8b

<!-- linear-runner demo-batch/TEAM-12/blocked/1 -->

---

## 10. Blocked or stopped, review not accepted

Template `blocked.md`, written by the runner (quotes the reviewer), posted when the batch pauses on a rejected review.

---

TEAM-12 is paused because the independent review did not accept it, and it needs your decision to continue.

**What happened**
The independent review of the committed work was not accepted, so nothing was published. The runner reported: Independent acceptance is incomplete.

**In their own words**
The reviewer wrote:

> The QC report has no error bars on the per-tile counts, which the first criterion requires.

Criteria the reviewer marked as not met:
- The QC report shows per-tile spot counts with error bars: qc_report.html shows counts without error bars

**What is needed**
Decide whether the reviewer is right. You can re-run only the review (with a note or a clarified criterion), resume the worker, or set the issue aside.

**To continue**
Record a review-only recovery (you can add --note-file with a note for the reviewer, or --repin-contract after clarifying a criterion):

```bash
python3 /absolute/path/to/linear-runner/runner.py recover review --batch demo-batch --reason "<why>" --authorized-by "<your name>"
```

Then start the batch again:

```bash
python3 /absolute/path/to/linear-runner/runner.py launch --batch demo-batch
```

Or set the issue aside instead and let the batch continue:

```bash
python3 /absolute/path/to/linear-runner/runner.py recover defer --issue TEAM-12 --restore-worktree --batch demo-batch --reason "<why>" --authorized-by "<your name>"
```

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d, /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d/review-20260923T110000Z-4d5e6f7a

<!-- linear-runner demo-batch/TEAM-12/blocked/2 -->

---

## 11. Blocked or stopped, environment

Template `blocked.md`, written by the runner, posted when a host or service problem pauses the batch.

---

TEAM-12 is paused by a host or service problem (Linear OAuth expired; refresh with the owning CLI and resume), and it needs you to fix it and relaunch.

**What happened**
The runner stopped at the validate step with this message: Linear OAuth expired; refresh with the owning CLI and resume.

**What is needed**
Fix the host or service problem (for example refresh the Linear login or free disk space), then resume.

**To continue**
Record the recovery (you can add --note-file with a note for the worker):

```bash
python3 /absolute/path/to/linear-runner/runner.py recover resume --batch demo-batch --reason "<why>" --authorized-by "<your name>"
```

Then start the batch again:

```bash
python3 /absolute/path/to/linear-runner/runner.py launch --batch demo-batch
```

Evidence: /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d, /absolute/path/to/controller/demo-batch/state.json

<!-- linear-runner demo-batch/TEAM-12/blocked/3 -->

---

## 12. Issue deferred

Template `deferred.md`, written by the runner, posted when a decision rule or on_block policy sets an issue aside.

---

TEAM-13 was set aside after it blocked and the batch continues with independent issues; you can restore it later.

**Why**
The issue's own decision rule "defer issue when worker blocked 2 times on the same criterion" matched block TEAM-13#2. It blocked at the implement step: Worker reported blocked: the viewer needs a browser the host does not have.

**In their own words**
The worker wrote:

> The viewer check needs a headless browser, and none is installed on the host.

Criteria the worker marked as not met:
- The viewer opens the QC report: no browser on the host

**To restore it later**
It is not accepted and keeps its Linear state.

Once the batch has stopped, record the restore:

```bash
python3 /absolute/path/to/linear-runner/runner.py recover resume --issue TEAM-13 --batch demo-batch --reason "<why>" --authorized-by "<your name>"
```

Then start the batch again:

```bash
python3 /absolute/path/to/linear-runner/runner.py launch --batch demo-batch
```

Evidence: /absolute/path/to/runs/TEAM-13/20260923T130000Z-2b3c4d5e

<!-- linear-runner demo-batch/TEAM-13/deferred/1 -->

---

## 13. Recovery recorded

Template `recovery.md`, written by the runner, posted when a launch carries out a recorded recovery.

---

A resume recovery for TEAM-12 recorded by Owner is being carried out now; no action is needed.

**Recovery**
The runner resumes TEAM-12 from its saved step (implement). The reason given was: The calibration table is at data/calibration.csv; the note tells the worker.

**Then**
After this issue the batch continues.

**Owner note**
> The calibration table is data/calibration.csv; read it, do not recreate it.

Evidence: /absolute/path/to/controller/demo-batch/recovery-log.jsonl

<!-- linear-runner demo-batch/TEAM-12/recovery/1 -->

---

## 14. Batch finished

Template `batch-finished.md`, written by the runner, posted on the terminal issue and report issues when the batch ends.

---

Batch demo-batch finished with 3 of 5 issues Done, and the rest need your decision.

**Issues**
Done: TEAM-10, TEAM-11 and TEAM-12. Set aside: TEAM-13. Waiting on prerequisites: TEAM-14 (needs TEAM-13).

**Usage**
About 18.4M input tokens (15.9M cached) and 212k output tokens over 7 model sessions; these are counters, not billed cost.

**To continue**
Restore a set-aside issue:

```bash
python3 /absolute/path/to/linear-runner/runner.py recover resume --issue <issue> --batch demo-batch --reason "<why>" --authorized-by "<your name>"
```

Then start the batch again:

```bash
python3 /absolute/path/to/linear-runner/runner.py launch --batch demo-batch
```

Evidence: /absolute/path/to/controller/demo-batch/terminal-report.html, /absolute/path/to/controller/demo-batch/terminal-report.json

<!-- linear-runner demo-batch/TEAM-14/batch-finished/1 -->

---

## 15. Batch paused

Template `batch-paused.md`, written by the runner, posted on the terminal issue and report issues when the batch pauses.

---

Batch demo-batch is paused on TEAM-12 and needs your attention; the details are in the comment on TEAM-12.

**Issues**
Done: TEAM-10 and TEAM-11. Paused: TEAM-12. Not started: TEAM-13 and TEAM-14.

**To continue**
Follow the steps in the comment on TEAM-12.

Evidence: /absolute/path/to/controller/demo-batch/terminal-report.html, /absolute/path/to/controller/demo-batch/terminal-report.json

<!-- linear-runner demo-batch/TEAM-14/batch-paused/1 -->

---

## 16. Watchdog alert

Template `watchdog.md`, written by the runner (model-free watchdog), posted when the supervisor vanished without an outcome, or stalled.

---

The supervisor for batch demo-batch stopped without reporting an outcome while working on TEAM-12, and it needs you to check the host and relaunch.

**What the watchdog saw**
supervisor.json still says running (launch L-20260923T090000Z-7f8e9d0c, PID 48121 on build-host), but that process no longer exists and no terminal outcome was written. The last recorded progress was at 2026-09-23 10:42 UTC.

**Last update**
> From the progress comment: The spot-count section of the QC report now renders for all tiles; no action is needed.

**To continue**
Check the saved state and the supervisor:

```bash
python3 /absolute/path/to/linear-runner/runner.py status --batch demo-batch
```

If nothing is running, record a recovery:

```bash
python3 /absolute/path/to/linear-runner/runner.py recover resume --batch demo-batch --reason "<why>" --authorized-by "<your name>"
```

Then start the batch again:

```bash
python3 /absolute/path/to/linear-runner/runner.py launch --batch demo-batch
```

Evidence: /absolute/path/to/controller/demo-batch/supervisor.json, /absolute/path/to/controller/demo-batch/supervisor.log, /absolute/path/to/runs/TEAM-12/20260923T101500Z-1a2b3c4d

<!-- linear-runner demo-batch/TEAM-12/watchdog/1 -->

---

## 17. Done, nothing to review

Template `done.md`, written by the runner, posted after Done is published and read back.

---

TEAM-11 is Done: the independent review accepted all 2 acceptance criteria and Linear shows the issue Done, so no action is needed.

**Delivered**
Commit 9a8b7c6d5e4f on branch codex/demo-batch, local only (not pushed or merged).

Evidence: /absolute/path/to/runs/TEAM-11/20260923T081500Z-0f1e2d3c, /absolute/path/to/runs/TEAM-11/20260923T081500Z-0f1e2d3c/final-result.json

<!-- linear-runner demo-batch/TEAM-11/done/1 -->

---
